"use client";
import {useState} from "react";
export function AiPlayground(){
 const [secret,setSecret]=useState(""),[text,setText]=useState("Explique em uma frase o que é uma API."),[result,setResult]=useState(""),[busy,setBusy]=useState(false);
 async function run(){setBusy(true);setResult("");try{const r=await fetch("/api/v1/ai/chat/completions",{method:"POST",headers:{Authorization:`Bearer ${secret.trim()}`,"Content-Type":"application/json","Idempotency-Key":crypto.randomUUID()},body:JSON.stringify({model:"connectyhub-auto",max_tokens:256,messages:[{role:"user",content:text}]})});const d=await r.json();setResult(JSON.stringify(d,null,2));}catch{setResult("Conexão interrompida. Consulte as atividades antes de enviar uma nova chamada.");}finally{setBusy(false);}}
 return <details className="rounded-2xl border border-slate-200 bg-white p-5"><summary className="cursor-pointer text-lg font-bold">Testar uma chamada</summary><p className="my-3 text-sm leading-6 text-slate-600">O teste usa a API real e consome créditos conforme a tarifa do modelo. A chave permanece apenas nesta tela durante o teste. A resposta tem limite de 256 tokens.</p><form className="space-y-4" onSubmit={e=>{e.preventDefault();void run();}}><label className="block text-sm">Chave do projeto<input required autoComplete="off" type="password" value={secret} onChange={e=>setSecret(e.target.value)} placeholder="chy_ai_…" className="mt-2 min-h-11 w-full rounded-xl border border-slate-300 px-3"/></label><label className="block text-sm">Mensagem<textarea required maxLength={3000} value={text} onChange={e=>setText(e.target.value)} className="mt-2 min-h-24 w-full rounded-xl border border-slate-300 p-3"/></label><button disabled={busy} className="min-h-11 rounded-xl bg-blue-700 px-4 text-sm font-semibold text-white hover:bg-blue-800 disabled:opacity-50">{busy?"Processando…":"Enviar teste com meus créditos"}</button></form>{result&&<pre className="mt-4 max-h-96 overflow-auto rounded-xl bg-slate-950 p-4 text-xs leading-6 text-slate-100">{result}</pre>}</details>;
}
