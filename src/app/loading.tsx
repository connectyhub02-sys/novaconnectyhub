import { InfinityLoadingScreen } from "@/components/connectyhub-os/infinity-loader";

export default function Loading() {
  return <InfinityLoadingScreen label="Carregando…" description="Só um instante, estamos abrindo a página." eyebrow="ConnectyHub" />;
}
