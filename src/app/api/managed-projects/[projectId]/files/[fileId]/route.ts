import {managedSession,managedFailure,unwrap,id,ManagedError} from '@/lib/managed-projects/server';
export async function GET(_request:Request,{params}:{params:Promise<{projectId:string;fileId:string}>}){try{const {db}=await managedSession();const p=await params;const data=unwrap(await db.rpc('managed_read_file',{p_project:id(p.projectId),p_file:id(p.fileId)}));if(data===null)throw new ManagedError(404,'Arquivo não encontrado.');return new Response(Buffer.from(data,'base64'),{headers:{'Content-Type':'application/octet-stream','Content-Disposition':'attachment; filename="arquivo"','Cache-Control':'no-store','X-Content-Type-Options':'nosniff'}});}catch(e){return managedFailure(e);}}
