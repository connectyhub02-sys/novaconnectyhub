export function leadContactPage(state: "confirm" | "disabled" | "missing" | "error", key = "") {
  const title = state === "disabled" ? "Você saiu da lista" : state === "missing" ? "Link não encontrado" : state === "error" ? "Não foi possível concluir" : "Sair da lista de contatos automáticos?";
  const description = state === "disabled" ? "Sua escolha foi registrada. Os próximos follow-ups, lembretes de agenda, retornos e mensagens de recuperação desta empresa serão interrompidos. Uma mensagem já em envio pode ainda chegar."
    : state === "confirm" ? "Ao confirmar, você deixa de receber os follow-ups, lembretes de agenda, retornos e mensagens de recuperação desta empresa pelo WhatsApp."
    : state === "missing" ? "Confira o link na mensagem que você recebeu." : "Sua escolha não foi confirmada. Tente novamente em alguns instantes.";
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${title} | ConnectyHub</title><style>body{margin:0;padding:24px;background:#f5f7fb;color:#172033;font:16px/1.6 system-ui}main{max-width:520px;margin:8vh auto;padding:clamp(20px,5vw,40px);background:white;border:1px solid #dfe5ef;border-radius:20px}strong{color:#1744eb}h1{font-size:28px;line-height:1.25}p{color:#475569}button{width:100%;border:0;border-radius:10px;padding:14px 20px;background:#1744eb;color:white;font:600 16px system-ui;cursor:pointer}button:focus-visible{outline:3px solid #87aaff;outline-offset:4px}small{display:block;margin-top:20px;color:#64748b}</style></head><body><main><strong>CONNECTYHUB</strong><h1>${title}</h1><p>${description}</p>${state === "confirm" ? `<form method="post" action="/contato/preferencias/${key}"><input type="hidden" name="action" value="unsubscribe"><button>Confirmar saída da lista</button></form>` : ""}<p>Sair da lista não cancela seus agendamentos ou pedidos. Você pode continuar falando com a empresa por esta conversa.</p><small>Abrir este link não altera sua preferência. Uma nova mensagem sua não reativa os contatos automáticos.</small></main></body></html>`;
}
